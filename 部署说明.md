# 部署说明

## 1. 启动中间件（Docker）

项目根目录提供了 `docker.sh`，一键启动 PostgreSQL（pgvector）和 EMQX：

```bash
# 启动 PostgreSQL 17 + pgvector 扩展
docker run -d \
  --name pg17-vector \
  -p 5432:5432 \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=123456 \
  -v /data/pg17-vector:/var/lib/postgresql/data \
  pgvector/pgvector:pg17

# 启动 EMQX（MQTT broker）
docker run -d --name emqx -p 1883:1883 -p 18083:18083 emqx:latest
```

> 可根据实际情况修改端口映射和密码。

## 2. 初始化数据库

`sql/` 目录包含 3 个脚本，按顺序执行：

| 文件 | 说明 |
|------|------|
| `schema.sql` | 建表脚本，创建全部 7 张表并预置默认阈值 |
| `test-data.sql` | 测试数据，插入示例用户、设备、光照记录等 |
| `update-vector-dimension.sql` | 向量维度调整（1024 → 1536），适配 text-embedding-3-small |

```bash
# 连接 PostgreSQL 后依次执行
psql -U postgres -d smart-street-light -f sql/schema.sql
psql -U postgres -d smart-street-light -f sql/test-data.sql
psql -U postgres -d smart-street-light -f sql/update-vector-dimension.sql
```

## 3. 修改配置文件

`src/main/resources/application.yml` — 修改数据库和 MQTT 连接地址：

```yml
spring:
  datasource:
    url: jdbc:postgresql://<你的IP>:5432/smart-street-light
    username: postgres

mqtt:
  broker-url: tcp://<你的IP>:1883
```

`src/main/resources/application-secret.yml` — 配置密码和密钥（参考同目录下的 `application-secret-example.yml`）：

```yml
spring:
  datasource:
    password: 123456

jwt:
  secret-key: 随机字符串作为JWT密钥（最好长一点不然会报安全错误）

llm:
  api-key: 你的LLM-API-Key
  base-url: https://api.openai.com
  model: gpt-4o-mini
  embedding-model: text-embedding-3-small
  top-k: 3

mqtt:
  username: admin
  password: 123456
```

## 4. 构建启动

```bash
mvn package -DskipTests
java -jar target/smart-street-light-0.0.1-SNAPSHOT.jar
```

应用启动后监听 **8080** 端口。
